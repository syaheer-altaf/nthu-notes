This repository serves as an archive of my notes from courses I took during my postgraduate studies at National Tsing Hua University.

Simply run the following command and check for the `.pdf` files in the `./compiled` directory:

```bash
sudo docker compile run latex```
